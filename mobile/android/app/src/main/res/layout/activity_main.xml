<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/camera_background">

    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:implementationMode="compatible"
        app:scaleType="fillCenter" />

    <View
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:layout_gravity="top"
        android:background="@drawable/bg_camera_bottom_scrim"
        android:rotation="180" />

    <!-- Full-width top bar (not a floating card) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:background="@drawable/bg_header_full"
        android:elevation="6dp"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingTop="28dp"
        android:paddingEnd="20dp"
        android:paddingBottom="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:contentDescription="@string/cd_hand_logo"
                android:src="@drawable/asl_logo" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:fontFamily="sans-serif-medium"
                android:letterSpacing="0.04"
                android:text="@string/welcome_brand"
                android:textColor="@color/text_headline"
                android:textSize="16sp" />

            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/toggleWordModeButton"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:contentDescription="@string/cd_toggle_word_mode"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:minHeight="36dp"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:text="@string/mode_word_enable"
                android:textColor="@color/primary"
                android:textSize="13sp"
                app:cornerRadius="@dimen/word_toggle_corner"
                app:rippleColor="@color/primary_light"
                app:strokeColor="@color/word_toggle_outline"
                app:strokeWidth="1dp" />
        </LinearLayout>

        <TextView
            android:id="@+id/topMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:background="@drawable/bg_message_full"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="3"
            android:paddingStart="12dp"
            android:paddingTop="10dp"
            android:paddingEnd="12dp"
            android:paddingBottom="10dp"
            android:textColor="@color/text_headline"
            android:textSize="14sp"
            android:visibility="gone"
            tools:text="Getting ready…"
            tools:visibility="visible" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:layout_gravity="bottom"
        android:background="@drawable/bg_camera_bottom_scrim" />

    <!-- Full-width bottom bar (edge-to-edge, not a card) -->
    <LinearLayout
        android:id="@+id/predictionCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@drawable/bg_letter_bar"
        android:elevation="10dp"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingTop="10dp"
        android:paddingEnd="16dp"
        android:paddingBottom="12dp">

        <TextView
            android:id="@+id/predLetter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:includeFontPadding="false"
            android:text="—"
            android:textColor="@color/primary"
            android:textSize="52sp"
            tools:text="A" />

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/wordModePanel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:visibility="gone"
            app:cardBackgroundColor="@color/word_panel_bg"
            app:cardCornerRadius="@dimen/word_panel_corner"
            app:cardElevation="0dp"
            app:strokeColor="@color/word_panel_stroke"
            app:strokeWidth="1dp"
            tools:visibility="visible">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="@dimen/word_panel_padding">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <TextView
                        android:layout_width="@dimen/word_panel_label_width"
                        android:layout_height="wrap_content"
                        android:fontFamily="sans-serif-medium"
                        android:letterSpacing="0.1"
                        android:text="@string/word_label_spelling"
                        android:textAllCaps="true"
                        android:textColor="@color/word_label_muted"
                        android:textSize="10sp" />

                    <TextView
                        android:id="@+id/spellingText"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:fontFamily="sans-serif-medium"
                        android:gravity="end"
                        android:includeFontPadding="false"
                        android:letterSpacing="0.08"
                        android:text="@string/word_spelling_empty"
                        android:textColor="@color/word_spelling_text"
                        android:textSize="17sp"
                        tools:text="HELLO" />
                </LinearLayout>

                <View
                    android:layout_width="match_parent"
                    android:layout_height="1dp"
                    android:layout_marginTop="6dp"
                    android:layout_marginBottom="4dp"
                    android:background="@color/word_panel_divider" />

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:minHeight="20dp"
                    android:orientation="horizontal">

                    <TextView
                        android:layout_width="@dimen/word_panel_label_width"
                        android:layout_height="wrap_content"
                        android:fontFamily="sans-serif-medium"
                        android:letterSpacing="0.1"
                        android:text="@string/word_label_match"
                        android:textAllCaps="true"
                        android:textColor="@color/word_label_muted"
                        android:textSize="10sp" />

                    <TextView
                        android:id="@+id/wordMatchText"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:ellipsize="end"
                        android:fontFamily="sans-serif"
                        android:gravity="end"
                        android:maxLines="2"
                        android:textColor="@color/word_result_muted"
                        android:textSize="13sp"
                        tools:text="Word: hello" />
                </LinearLayout>

                <TextView
                    android:id="@+id/wordFormationHint"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:ellipsize="end"
                    android:fontFamily="sans-serif"
                    android:maxLines="2"
                    android:text="@string/word_formation_hint"
                    android:textColor="@color/word_result_muted"
                    android:textSize="11sp" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/confirmWordButton"
                    style="@style/Widget.Sign2Text.WordConfirm"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="@string/word_button_confirm" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <TextView
            android:id="@+id/hintText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:gravity="center"
            android:lineSpacingMultiplier="1.2"
            android:text="@string/hint_idle"
            android:textColor="@color/text_secondary_light_bg"
            android:textSize="13sp" />
    </LinearLayout>
</FrameLayout>
